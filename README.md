# Meta Publisher MCP

Node.js + TypeScript MCP server to publish organic Facebook/Instagram posts and optional local-awareness Meta ads for A to Z Flooring Solutions.

1. Copy `.env.example` to `.env` and fill in Meta IDs and tokens.
2. Run `npm install`.
3. Run `npm run dev` to start the MCP server in watch mode.
